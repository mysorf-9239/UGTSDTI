name: "hybrid_dti"
params:
  student_cfg: null
  teacher_cfg:
    name: "baseline_teacher"
    params:
      hidden_dim: 128
      num_drugs: 1000   # set to dataset.num_unique_drugs at runtime
      num_targets: 1000 # set to dataset.num_unique_targets at runtime
  fusion_cfg: null
