extends:
  - ../base_full.yaml
sweep:
  parameters:
    interaction.kd.params.temperature:
      - 1.0
      - 2.0
      - 4.0
    loss.map.kd.weight:
      - 0.1
      - 0.3
      - 0.5
    decision.use_uncertainty:
      - true
      - false
    decision.strategy:
      - soft
      - hard
    training.teacher.freeze:
      - true
      - false
    modalities.student.uses:
      - [sequence]
      - [sequence, structure]
