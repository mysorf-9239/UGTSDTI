name: default_trainer
params:
  epochs: 100
  patience: 15
  val_check_interval: 1
  grad_clip: 1.0
  lr: 0.001
  batch_size: 32
  num_workers: 0
  output_dir: "./outputs/checkpoints"
loss: ${loss}
graph:
  warmup_epochs: 5
  rebuild_every: 10
  ema_decay: 0.99
