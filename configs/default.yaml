# Default Main Entry Configuration
defaults:
  - _self_
  - model: hybrid
  - teacher: baseline
  - student: baseline
  - fusion: ug
  - loss: bce
  - data: tdc_davis_s4
  - trainer: default_trainer

run_name: "ugtsdti_baseline"
seed: 42
strict_cudnn: false
log_level: "INFO"
output_dir: "./outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}"

logging:
  wandb_enabled: true
